
import 'package:flutter/material.dart';

class NumberDivider extends StatelessWidget {
  const NumberDivider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 24,
      child: VerticalDivider(

      color: Colors.black,
      ),
    );
  }
}
